#include <bits/stdc++.h>
using namespace std;
int main(){
    vector<int> arr;
    arr.push_back(1);
    arr.push_back(4);
    arr.push_back(2);
    arr.push_back(3);
    for (int i = 0; i < arr.size(); i++)
    {
        cout<<arr[i]<<endl;
    }
    cout<<endl<<endl;
    // arr.pop_back();
    // arr.clear();
    // arr.insert(arr.begin(), 99);
    // arr.insert(arr.begin()+1, 99);
    // arr.insert(arr.end(), 99);
    // arr.erase(arr.begin());
    // arr[1]=99;

    for (int i = 0; i < arr.size(); i++)
    {
        cout<<arr[i]<<endl;
    }
    
}